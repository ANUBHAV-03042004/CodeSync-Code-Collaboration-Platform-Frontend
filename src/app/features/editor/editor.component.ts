// Re-export the real component so TypeScript resolves this file correctly
// when it is included in compilation alongside the real source.
export { EditorComponent } from '../features/editor/editor.component';
